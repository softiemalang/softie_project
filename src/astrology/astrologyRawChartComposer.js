import { composeAstrologyRawChart } from './astrologyEphemerisCore.js'

/** Connects a previously derived Time & Angle result to the research ephemeris core. */
export function composeAstrologyRawChartV0(input) {
  return composeAstrologyRawChart(input)
}
