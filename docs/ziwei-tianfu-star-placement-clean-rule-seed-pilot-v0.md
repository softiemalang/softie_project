# Ziwei Tianfu source-rule seed pilot v0

This is a bounded provenance and calculation reconciliation artifact, not a personal-meaning claim, truth validation, readiness decision, grounding, or activation.

The source witness is the actual PDF `/Users/softie/Downloads/命-南北山人_紫微斗数全书.pdf`, SHA-256 `4786a94ab454acdabf9716d7c0db4756dbcbde99a88bc45fda254863c1961023`, 219 pages, unencrypted. The selected range is PDF page 13, printed page 三十四, scanned right leaf, section `甲六、安天府`. The two-column table is preserved with row locators in `transcription.json`; OCR is `exploration_only_not_canonical`, and modern commentary is excluded.

The direct domain is the 12 source table inputs `紫微所在地支 -> 天府所在地支`. The integrated domain is the accepted 150-row bureau-number × lunar-day domain. Its production Ziwei branch is observed through `resolve14MajorStars`, then passed to the independent source evaluator; only production `majorStars[id=tianfu].palaceBranch` is compared. The source evaluator has no production, fixture, or lookup-table imports. Source mapping, normalization, adapter, and production results remain separate.

The pilot preserves all row IDs, intermediate values, upstream provenance, source and production positions, matches, mismatches, first divergence, branch distribution, and parallel possible causes. It does not expand to other major stars or modify production rules or existing artifacts. The status boundary remains stable claim count 0, readiness `not_safe_to_start`, grounding `blocked`, activation `experimental`.
