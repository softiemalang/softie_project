# Ziwei Tianfu placement discrepancy analysis v0

This additive analysis re-reads only PDF p13 / printed page 三十四 / `甲六、安天府` on the right scanned leaf. It preserves the existing divergent pilot and does not decide source or production lineage.

The source-first visual transcription is 紫微 `子..亥` to 天府 `辰, 卯, 寅, 丑, 子, 亥, 戌, 酉, 申, 未, 午, 巳`. The review is bounded by the recorded PDF SHA-256, page count/encryption state, render tool/DPI/crop/hash, and the declared absence of independent human review.

The full direct 12-row candidate matrix tests identity, all fixed rotations, all reflection+rotations, inverse mapping, all bounded input/output enum relabel pairs, and source base/direction. Every candidate tests every row. Exact fits are preserved as a set; no rank is assigned. The source mapping has a fixed rotation `source = production + 6 (mod 12)` and the equivalent bounded enum relabelings. The same prediction explains all 150 integrated rows, while the original 25/125 values and first mismatch remain unchanged in a separate field.

This is relation evidence only. Stable claims remain 0, readiness `not_safe_to_start`, grounding `blocked`, and activation `experimental`.
