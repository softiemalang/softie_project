# Saju source claim observation successor v1

This additive artifact records a direct visual observation from page 5 of the
local `子平真诠` PDF. The visible heading and nearby sentences define the
page's `刑`, `冲`, `会`, and `合` categories and mention `三方` and `并对`.
The transcription is manually made from a rendered page; OCR is not canonical.

The local file's bytes and page count are checked directly, but its edition and
transmission identity remain unresolved. Example tables are intentionally not
transcribed because their layout boundaries were not admitted. The linked 23
branch-relation claims remain unverified, the stable claim count remains zero,
and no production rule, readiness, grounding, interpretation, or activation
state changes.

```text
node scripts/materialize-saju-source-claim-observation-v1.mjs
node scripts/check-saju-source-claim-observation-v1.mjs
```
