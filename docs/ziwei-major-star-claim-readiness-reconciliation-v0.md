# Ziwei major-star claim/readiness reconciliation v0

Verdict: `complete_ziwei_major_star_claim_readiness_reconciliation_evidence_uncommitted`.

This additive packet normalizes 14 narrow placement-rule evidence claims. It does not create traditional-truth, personal-meaning, priority, ranking, weighting, interpretation, or production-selection claims.

The preserved contexts are distinct: integrated baseline `25 match / 125 mismatch`; Tianfu neutral raw `0/150 exact`; Tianfu `rotation-06` transform `150/150`, residual `0`; and the source corpus `219/219` direct review, gap `0`. No denominator is merged.

Claim statuses are one `evidence_sufficient_within_scope` (紫微 coordinate comparison), one `transform_verified_semantics_unresolved` (天府), and twelve `source_scope_exhausted_unresolved`. Every claim has artifact byte references, calculation/Git provenance, context, blockers, and `not_eligible_for_interpretation`.

The relation graph contains supported-by, derived-from, exact-match, transform-equivalent, context-differs, blocked-by, and unresolved-source edges. The blocker registry records the missing direct rules for 12 stars and missing palace label/ordinal identity evidence. The source-acquisition specification defines required document, rule, correspondence, and edition-comparison evidence without external search.

Layered readiness remains local-research/audit only within scope; semantic readiness is blocked, grounding is blocked, activation is experimental, interpretation and production choice are forbidden/not performed. Existing artifacts and production rules are protected by actual-byte hashes.
