# Ziwei ming/shen seed acceptance v0

This additive review accepts the seed with declared limits at fixed HEAD `32cc3ac45f94e40fbcda70c63d0d2d4b8509112c`. Reviewer B rendered PDF pages 8 and 10 from the byte-identified, 219-page, unencrypted PDF and read the target printed leaves 二十五 and 二十九 before opening the pilot artifacts. Rendered images and the PDF remain outside Git.

The only rule-bearing A/B difference is the uncertain first phrase glyph on page 8 (`寅正巳順數生月逢` in A versus B's visible reading `寅正順數生月逢`). It is recorded as `uncertain but rule-neutral`; the following explanation supports 寅 start, month progression, 命宮 reverse, and 身宮 forward. Page 10's auxiliary matrix is recorded as additional layout-aware evidence, not promoted into the seed rule. No semantic discrepancy was found, so the pilot, normalized rule, production resolver, and their three hashes remain unchanged.

The direct source trace is deliberately separate from implementation normalization: the source supports 寅 as the first month branch and the 命/身 directions, while the complete 1..12 domains, modulo arithmetic, and `命宮 -> life` / `身宮 -> shen` mapping are implementation facts. The acceptance reruns an independent source evaluator against the production evaluator for all 144 lunar-month/hour-branch combinations and requires 144/144, first divergence null, fixed ordering, byte-deterministic materialization, and unchanged pilot hashes.

This is source-backed rule reconciliation only. It keeps stable claims at 0, readiness `not_safe_to_start`, grounding `blocked`, and activation `experimental`.
