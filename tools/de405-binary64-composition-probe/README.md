# DE405 binary64 composition helper

This helper performs only binary64 add/subtract/negate operations from hexadecimal operands. It accesses no SPK or CSPICE APIs. It requires IEEE-754 binary64, round-to-nearest, and no fast-math build mode.
