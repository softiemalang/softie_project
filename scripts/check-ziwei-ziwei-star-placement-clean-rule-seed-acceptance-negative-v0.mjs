import { readFile } from 'node:fs/promises'
import { checkAcceptanceArtifact } from './check-ziwei-ziwei-star-placement-clean-rule-seed-acceptance-v0.mjs'
const candidate = JSON.parse(await readFile(process.argv[2] || 'artifacts/ziwei-ziwei-star-placement-clean-rule-seed-acceptance-v0/complete.json', 'utf8')); const mutations = { pdf_hash: c => { c.source.pdfSha256 = '0'.repeat(64) }, ocr_canonical: c => { c.reviewerB.ocrPolicy.canonical = true }, uncertain_glyph: c => { c.reviewerB.pages[0].uncertainty = [] }, table_direction: c => { c.reviewerB.pages[1].table.direction = 'transposed' }, semantic_collapse: c => { c.acceptance.semanticDiscrepancy = true }, pilot_overwrite: c => { c.acceptance.boundaries.pilotOverwritten = true }, production_reuse: c => { c.independence.sourceEvaluatorImportsProduction = true }, force_150: c => { c.comparison.matchCount = 150; c.comparison.rows[0].match = false }, promotion: c => { c.acceptance.boundaries.stableClaimCount = 1 }, git_render: c => { c.source.gitInclusion = 'included' }, id_order: c => { c.comparison.rows[0].rowId = c.comparison.rows[1].rowId }, head_rewrite: c => { c.basisHead = '0'.repeat(40) } }; const failures = []; for (const [id, mutate] of Object.entries(mutations)) { const copy = structuredClone(candidate); mutate(copy); const errors = await checkAcceptanceArtifact(copy); if (!errors.length) failures.push(id) } console.log(JSON.stringify({ pass: failures.length === 0, mutationCount: Object.keys(mutations).length, undetected: failures }, null, 2)); if (failures.length) process.exitCode = 1
